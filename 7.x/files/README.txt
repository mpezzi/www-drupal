Use this as the Drupal uploaded files directory (e.g. sites/default/files). 

This is optional as your files directory may be located on a network mount (as is the case with Acquia Hosting, for example) or on some other system resource.